import React from 'react'
import { storiesOf } from '@storybook/react'
import QuestTitle from '.'

storiesOf('QuestTitle', module).add('default', () => (
  <QuestTitle title="Save Rasheed - Spatial 2018" />
))
